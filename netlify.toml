import { initializeApp } from 'firebase/app';

// Mock netlify.toml content in comments for reference
/*
[build]
command = "npm run build"
publish = "dist"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
*/
